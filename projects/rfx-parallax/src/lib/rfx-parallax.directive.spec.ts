import { RfxParallaxDirective } from './rfx-parallax.directive';

describe('RfxParallaxDirective', () => {
  it('should create an instance', () => {
    // const directive = new RfxParallaxDirective();
    // expect(directive).toBeTruthy();
  });
});
