import { SortImagesByPriorityPipe } from './sort-images-by-priority.pipe';

describe('SortImagesByPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new SortImagesByPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
