import { RfxImageLoaderDirective } from './rfx-image-loader.directive';

describe('RfxImageLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new RfxImageLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
